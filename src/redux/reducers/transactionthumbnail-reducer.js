import { TransactionThumbnail } from "../../models/transactionthumbnail-model";

const initialState = {
    transactionthumbnail:TransactionThumbnail,
    allTransactionThumbnails:[]
}

const transactionThumbnailReducer = (state = initialState, action) => {
    return state;
}

export default transactionThumbnailReducer;