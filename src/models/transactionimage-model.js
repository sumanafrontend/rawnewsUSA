class TransactionImage {
    constructor(imageId, base64, transactionId, createdAt) {
      (this.imageId = imageId),
        (this.base64 = base64),
        (this.transactionId = transactionId),
        (this.createdAt = createdAt);
    }
  }
  