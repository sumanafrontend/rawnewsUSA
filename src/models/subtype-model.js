class SubType {
    constructor(name, active, typeId, createdAt) {
      (this.name = name),
        (this.active = active),
        (this.typeId = typeId),
        (this.createdAt = createdAt);
    }
  }
  