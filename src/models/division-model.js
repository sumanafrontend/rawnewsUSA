class Division {
  constructor(shortName, name, city, unit, active, createdAt) {
    (this.shortName = shortName),
      (this.name = name),
      (this.city = city),
      (this.unit = unit),
      (this.active = active),
      (this.createdAt = createdAt);
  }
}
