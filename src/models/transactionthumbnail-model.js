class TransactionThumbnail {
    constructor(thumbnailId, base64, createdAt) {
      (this.thumbnailId = thumbnailId),
        (this.base64 = base64),
        (this.createdAt = createdAt);
    }
  }
  