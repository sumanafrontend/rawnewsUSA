class Type {
  constructor(userId, userName, loggedId, loggedOut, ipAddress, createdAt) {
    (this.userId = userId),
      (this.userName = userName),
      (this.loggedId = loggedId),
      (this.loggedOut = loggedOut),
      (this.ipAddress = ipAddress),
      (this.createdAt = createdAt);
  }
}
