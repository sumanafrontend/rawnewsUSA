class SystemConfig {
    constructor(systemId, password, systemIp, lastLoginTime, createdAt) {
      (this.systemId = systemId),
        (this.password = password),
        (this.systemIp = systemIp),
        (this.lastLoginTime = lastLoginTime),
        (this.createdAt = createdAt);
    }
  }
  